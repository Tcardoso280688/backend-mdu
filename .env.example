# === Back-end MDU (exemplo) ===
FLASK_ENV=production
SECRET_KEY=troque-esta-chave
JWT_SECRET_KEY=troque-esta-chave-jwt
# Para desenvolvimento local (SQLite)
DATABASE_URL=sqlite:///mdu.db
# Para usar Supabase (Postgres), troque por algo como:
# DATABASE_URL=postgresql://USER:PASSWORD@HOST:PORT/DBNAME?sslmode=require

# Domínio do seu frontend (Netlify) para CORS
NETLIFY_ORIGIN=https://quebrarsc.netlify.app

# Admin padrão criado no primeiro start (se não existir)
ADMIN_NAME=Admin
ADMIN_EMAIL=admin@mdu.com
ADMIN_PASSWORD=123456
ADMIN_ROLE=admin
